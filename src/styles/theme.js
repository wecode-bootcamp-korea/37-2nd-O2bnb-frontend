const color = {
  white: "#FFF",
  pink: "#FF385C",
  black: "#201f1f",
  grey: "#5c5b5b",
};

const flexCenter = () => {
  return `display: flex;
  justify-content: center;
  align-items: center;`;
};

const theme = {
  color,
  flexCenter,
};

export default theme;
